# Python internals
__pycache__/
*.py[cod]
*.so

# Virtual Environment
venv/
env/
.env

# IDE settings
.vscode/
.idea/

# Logs
*.log
cbc_procure.log

# Large Data & Cache (IMPORTANT)
data/hf_cache/
data/raw/


# 1. Ignore thư mục artifacts ở root (nếu có)
artifacts/

# 2. Xử lý thư mục data/
# Thay vì ignore "data/", ta ignore "data/*" (tất cả nội dung bên trong)
# Nhưng phải giữ lại chính folder data để Git có thể chui vào trong check tiếp
data/*
!data/

# 3. Đi vào trong data/, ta muốn giữ lại artifacts để đi tiếp
!data/artifacts/

# 4. Vào trong data/artifacts/, ta lại ignore tất cả
data/artifacts/*

# 5. CUỐI CÙNG: Giữ lại analysis_report (whitelist)
!data/artifacts/analysis_report/

# -----------------------------------------------------------
# GLOBAL EXTENSION IGNORE
# -----------------------------------------------------------
*.parquet
*.csv
*.log
*.png
*.svg

# LƯU Ý QUAN TRỌNG:
# Nếu trong folder "baselines" của bạn có chứa file .csv hay .parquet,
# thì nó vẫn bị ignore bởi rule *.csv ở trên.
# Nếu muốn giữ cả file csv trong folder report, hãy bỏ comment dòng dưới:
!data/artifacts/analysis_report/**/*.csv
!data/artifacts/analysis_report/**/*.png